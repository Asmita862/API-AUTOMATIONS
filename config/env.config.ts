
/*import dotenv from 'dotenv';

// Load .env file
dotenv.config();

// Create a typed configuration object
export const config = {
  api: {
    baseUrl: process.env.BASE_URL
  },
  admin: {
    credentials: {
      email: process.env.ADMIN_EMAIL,
      password: process.env.ADMIN_PASSWORD,
      invalidPassword: process.env.ADMIN_INVALID_PASSWORD
    },
    searchData: {
      firstName: process.env.ADMIN_FIRSTNAME,
      lastName: process.env.ADMIN_LASTNAME,
      fullName: process.env.ADMIN_FULLNAME,
      emailFull: process.env.ADMIN_EMAIL_FULL,
      emailPartial: process.env.ADMIN_EMAIL_PARTIAL,
      specialChar: process.env.ADMIN_SPECIAL_CHAR,
      caseInsensitiveName: process.env.ADMIN_CASE_INSENSITIVE_NAME
    }
  }
};

*/
